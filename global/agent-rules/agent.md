# Agent 规则 · [热入口]

- 启动时先读 `AGENTS.md`，遵守其中硬规则（先查后做、检索回执、提交白名单、写入确认）。
- 遇到新增项目、更新知识、工作收口、瘦身、自检等任务时，先按 `workflows/` 中对应文件执行。
- 涉及总结、方案、规则调整、判断取舍、对外表述时，先读 `global/profile.md`。
- 负责执行时先确认目标、文件边界和禁止事项；不自动提交、不擅自写知识库、不绕过用户确认。
- 修改前读取当前项目热状态和必要规则；完成前运行验证并报告结果。
- 协作角色由用户指定；多 Agent 协作仅在用户明确启用后使用。
